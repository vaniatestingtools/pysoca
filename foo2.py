def ow(r):
    for i in r:
        if len(r)>4:
            return 0
            if len(r) > 4:
                if len(r) > 4:
                    return 0
                else:
                    continue
            else:
                continue
        break
if __name__ == "__main__":
    print (ow([4]))
